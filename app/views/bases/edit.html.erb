<% provide(:title, "拠点修正") %>
<% provide(:class_text, 'new') %>
<h1>拠点情報修正</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @base, local: true) do |f| %>
      <%= render 'shared/base_error_messages' %>
      
      <%= f.label :base_number, class: "label-#{yield(:class_text)}" %>
      <%= f.number_field :base_number, class: "form-control" %>

      <%= f.label :base_name, class: "label-#{yield(:class_text)}" %>
      <%= f.text_field :base_name, class: "form-control" %>

      <%= f.label :attendance_type, class: "label-#{yield(:class_text)}" %>
      <%= f.text_field :attendance_type, class: "form-control" %>

      <%= f.submit "更新", class: "btn btn-primary btn-block btn-new" %>
    <% end %>
  </div>
</div>